import Grid from "./grid";

// define cell
test("updates cell to alive 1 || dead 0");
// updateSize
test("reduces grid in size || increases grid in size");
// create2darray
test("creates 2d array based on row and column arguements");
